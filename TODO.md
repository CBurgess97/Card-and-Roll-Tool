All items completed:
- [x] Choice style columns prompt user to select result (both standalone and prefix-suffix)
- [x] `roll table --check` validates files against the schema
- [x] `roll table --metadata` outputs table metadata
- [x] `roll table --print` outputs table in readable format
